from django.urls import path
from . import views

urlpatterns = [
    # Aquí puedes agregar las rutas relacionadas con la autenticación
    # Ejemplo:
    # path('login/', views.login_view, name='login'),
    # path('register/', views.register_view, name='register'),
]